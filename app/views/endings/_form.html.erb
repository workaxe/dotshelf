<%= form_for(@ending) do |f| %>
  <% if @ending.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ending.errors.count, "error") %> prohibited this ending from being saved:</h2>

      <ul>
      <% @ending.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
		<input type="text" name="ending[content]" value="<%= @ending.content %>" class="taggable_input">
  </div>
	<div class="field">
    <%= f.label :language %><br />
    <%= f.text_field :language %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
